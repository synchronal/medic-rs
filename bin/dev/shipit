#!/usr/bin/env bash
set -e
set -u
set -o pipefail

trap "exit" INT

source ".medic/_support/doctor.sh"

cargo run --bin medic -- shipit

# cecho --bright-green "\nChecksums:"
# cat tmp/release/*.sha256sum

