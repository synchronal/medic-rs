[doctor]

checks = [
  { check = "asdf", verbose = true, output = "json", command = "plugin-installed", args = { plugin = "no-plugin" } },
  { check = "asdf", command = "package-installed", args = { plugin = "no-plugin" } },
]
