[doctor]

checks = [
  { check = "tool-versions", verbose = true, output = "json", command = "plugin-installed", args = { plugin = "no-plugin" } },
  { check = "tool-versions", command = "package-installed", args = { plugin = "no-plugin" } },
]
